{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path | select(. != null)' | xargs -I {} npx prettier --write \"{}\""
          }
        ]
      }
    ]
  }
}
